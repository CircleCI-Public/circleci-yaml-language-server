version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout:
          method: shallow
      - run: echo "hello world"

workflows:
  test-build:
    jobs:
      - build
