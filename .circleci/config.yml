version: 2.1

commands:

jobs:
    build-and-test:
        docker:
            - image: cimg/go:1.19.1
        steps:
            - checkout

workflows:
    build:
        jobs:
            - build-and-test